import { Exercise1Directive } from './exercise1.directive';

describe('Exercise1Directive', () => {
  it('should create an instance', () => {
    const directive = new Exercise1Directive();
    expect(directive).toBeTruthy();
  });
});
